load("@rules_python//python:defs.bzl", "py_test")
load("@pybind11_bazel//:build_defs.bzl", "pybind_extension")

pybind_extension(
    name = "pybind_extension",
    srcs = ["pybind_extension.cc"],
    deps = ["//submodule"],
)
